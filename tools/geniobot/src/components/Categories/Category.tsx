import React, { FC } from 'react';
import { ICategory } from 'app-shared-types';

const Category: FC<ICategory> = ({ name, id, questions }) => {
	return (
		<div className="category">
			<h2>gg</h2>
		</div>
	);
};

export default Category;
